package com.github.alkedr.matchers.reporting.old;

public class IterableMatcher<T> extends IterableMatcherForExtending<T, IterableMatcher<T>> {
    public IterableMatcher(Type type) {
        super(type);
    }
}
