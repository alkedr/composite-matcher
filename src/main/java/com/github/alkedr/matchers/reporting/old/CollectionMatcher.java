package com.github.alkedr.matchers.reporting.old;

public class CollectionMatcher<T> extends CollectionMatcherForExtending<T, CollectionMatcher<T>> {
    public CollectionMatcher(Type type) {
        super(type);
    }
}
